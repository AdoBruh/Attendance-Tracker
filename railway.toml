[build]
builder = "railpack"

[deploy]
startCommand = "touch database/database.sqlite && php artisan storage:link --force && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
